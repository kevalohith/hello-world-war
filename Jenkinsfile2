pipeline {
    agent any
    stages {
        stage('Get Builder Node IP') {
            steps {
                script {
                    def builderNodeIP = sh(script: "hostname -I | awk '{print \$1}'", returnStdout: true).trim()
                    echo "Builder Node IP: ${builderNodeIP}"
                }
            }
        }
    }
}
